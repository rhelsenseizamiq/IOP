# Environment — NEVER commit real credentials
.env
.env.*
!.env.example
*.env.local

# Generated credentials — contains real passwords, NEVER commit
credentials.txt

# Internal planning / session files
PLAN.md

# Backup files — never intentional to commit
*.bak
*.bak.*

# Login-page branding — kept as a production-only local asset, not pushed
frontend/public/abb-logo.png

# SSL certificates / private keys — managed outside repo, never committed
nginx/ssl/
*.pem
*.key
*.crt
/_.abb-bank.az_2026.crt
/_.abb-bank.az_2026_key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
dist/
build/
*.egg-info/

# Node / Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Docker
*.log

# macOS
.DS_Store
